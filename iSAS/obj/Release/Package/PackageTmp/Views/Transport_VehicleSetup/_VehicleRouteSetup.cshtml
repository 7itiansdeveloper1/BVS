@model ISas.Entities.TransportEntities.VehicleRouteSetupModels

<div class="row">
    <div class="col-xs-12 col-sm-12 col-md-12">
        <div class="alert alert-info alert-dismissible" id="autoHideMsgDiv" style="z-index:1;position: absolute;min-width:98%;max-width:98%;display:none;">
            <h4><i id="autoHideDivTitleIcon" class="icon fa fa-info"></i> <span id="autoHideDivTitle"> Alert! </span> </h4>
            <p id="autoHideDivMsg"> Info alert preview. This alert is dismissable.</p>
        </div>
    </div>
</div>


@using (Html.BeginForm("VehicleRouteSetup_CRUD", "Transport_VehicleSetup", FormMethod.Post, htmlAttributes: new { }))
{
    @Html.HiddenFor(r => Model.VehicleId)
    <div class="row">
        <div class="col-md-12">
            <div style="max-height:300px;overflow:auto;">
                <table class="table table-bordered">
                    <thead>
                        <tr>
                            <th>@Html.CheckBox("checkBoxselectAllClasses", false, htmlAttributes: new { onchange = "Fun_checkToSelectAllClases()" }) Select Route </th>
                            <th>Route Name</th>
                        </tr>
                    </thead>
                    <tbody>
                        @for (int i = 0; i < Model.VehicleRouteDetails.Count; i++)
                        {
                            <tr>
                                <td>@Html.CheckBoxFor(r => Model.VehicleRouteDetails[i].Selected, htmlAttributes: new { @class = "checkBoxclass" })</td>
                                <td>
                                    @Html.HiddenFor(r => Model.VehicleRouteDetails[i].Value)
                                    @Html.DisplayFor(r => Model.VehicleRouteDetails[i].Text)
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
    <hr />
    <div class="row" style="border-top:1px solid #808080;">
        <div class="col-md-12">
            <button style="margin-top:2%;margin-right:1%;" type="button" onclick="CommonAjaxPost(this); setTimeout(function () { $('#mainModalWithSave').modal('toggle'); }, 1500);" class="btn btn-success">SAVE</button>
            <button style="margin-top:2%;" type="button" class="btn btn-default" data-dismiss="modal">CLOSE</button>
        </div>
    </div>
}
