@using ISas.Web.Models;
@model List<ISas.Entities.Academic.Academic_BankMasterModels>
@{
    int rowlength = 0;
    if (Model != null && Model.Count > 0)
    {
        rowlength = Model.Count;
    }
}
<!-- /.box -->
<div class="box box-primary">
    <!-- /.box-header -->
    <div class="box-header with-border">
        <div class="box-title">
            <h5 class="box-title">Bank List</h5>
        </div>
    </div>
    <div class="box-body">
        <table id="example1" class="table table-bordered table-striped">
            <thead>
                <tr>
                    <th>S. No</th>
                    <th>Bank</th>
                    <th>Action</th>
                </tr>
            </thead>
            <tbody>
                @if (Model != null && Model.Count > 0)
                {
                    int count = 1;
                    for (int i = 0; i < Model.Count(); i++)
                    {
                        <tr class="editable" id="editable_@i">
                            <td>
                                @Html.DisplayFor(r => count)
                                @Html.HiddenFor(r => Model[i].BankId, htmlAttributes: new { id = "BankId_" + @i })
                            </td>
                            <td>
                                <span style="display:none;"> @Html.DisplayFor(r => Model[i].BankName)</span>
                                @Html.TextBoxFor(r => Model[i].BankName, htmlAttributes: new { @class = "form-control editableFeild_" + @i + "", id = "BankName_" + @i, @readonly = true })
                            </td>
                            <td class="text-center">
                                <a href="#" onclick="EnableEditableRow('@i')" id="btnEdit_@i" style="" title="Edit Bank" class="fa fa-edit btnDefaultColor"></a>
                                <a href="#" onclick="UpdateBankMaster('@i')" id="btnSave_@i" style="display:none;" title="Save Bank" class="fa fa-check btnDefaultColor"></a>
                            </td>
                        </tr>
                        count++;
                    }
                }
            </tbody>
        </table>
    </div>
    <!-- /.box-body -->
</div>
<!-- /.box -->
<script>
    $(function () {
        $('#example1').DataTable();
        $('#hddnRowLenght').val('@rowlength');
    });
</script>