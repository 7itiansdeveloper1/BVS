@model List<ISas.Entities.Academic.Academic_QualificationMasterModels>
@{
    int rowlength = 0;
    if (Model != null && Model.Count > 0)
    {
        rowlength = Model.Count;
    }
}
<!-- /.box -->
<div class="box box-primary">
    <!-- /.box-header -->
    <div class="box-header with-border">
        <div class="box-title">
            <h5 class="box-title">Qualification List</h5>
        </div>
    </div>
    <div class="box-body">
        <table id="example1" class="table table-bordered table-striped">
            <thead>
                <tr>
                    <th>S. No</th>
                    <th>Qualification</th>
                    <th>Father Count</th>
                    <th>Mother Count</th>
                    <th>Staff Count</th>
                    <th>Action</th>
                </tr>
            </thead>
            <tbody>
                @if (Model != null && Model.Count > 0)
                {
                    int count = 1;
                    for (int i = 0; i < Model.Count(); i++)
                    {
                        <tr>
                            <td>
                                @Html.DisplayFor(r => count)
                                @Html.HiddenFor(r => Model[i].QualifId, htmlAttributes: new { id = "QualifId_" + @i })
                            </td>
                            <td>
                                <span style="display:none;"> @Html.DisplayFor(r => Model[i].QualifName)</span>
                                @Html.TextBoxFor(r => Model[i].QualifName, htmlAttributes: new { @class = "form-control editableFeild_" + @i + "", id = "QualifName_" + @i, @readonly = true })
                            </td>
                            <td>@Html.DisplayFor(r => Model[i].FatherStrength)</td>
                            <td>@Html.DisplayFor(r => Model[i].MotherStrength)</td>
                            <td>@Html.DisplayFor(r => Model[i].StaffStrength)</td>
                            <td class="text-center">
                                @if (Model[i].IsDeletable)
                                {
                                    <a href="#" onclick="DeleteQualificationConfirm('@Model[i].QualifId')"  title="Delete Qualification" class="fa fa-trash btnDefaultColor"></a>
                                }
                                else
                                {
                                    <span title="Delete Class" class="fa fa-trash btnDefaultColorFaded"></span>
                                }
                                <a href="#" onclick="EnableEditableRow('@i')" id="btnEdit_@i" title="Edit Qualification" class="fa fa-edit btnDefaultColor"></a>
                                <a onclick="UpdateQualification('@i')" id="btnSave_@i" style="display:none;" title="Save Qualification" href="#" class="fa fa-check btnDefaultColor"></a>
                            </td>
                        </tr>
                        count++;
                    }
                }
            </tbody>
        </table>
    </div>
    <!-- /.box-body -->
</div>
<!-- /.box -->
<script>
    $(function () {
        $('#example1').DataTable();
        $('#hddnRowLenght').val('@rowlength');
    });
</script>